import { expect, test } from "@playwright/test";

test.describe("SUITE 1: Visual testing", async () => {
   test("Verify capture screen - only view port", async ({ page }) => {});

   test("Verify capture screen - full page", async ({ page }) => {});

   test("Verify capture locator - header section", async ({ page }) => {});

   test("Verify capture locator - mask", async ({ page }) => {});
});
